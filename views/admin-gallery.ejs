<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Admin Galerie</title>
</head>
<body>

<h1>Gestion Galerie</h1>

<% if(images.length > 0){ %>

    <% images.forEach(image => { %>

        <div style="margin:20px;">

            <img src="<%= img.image %>" width="200">

        </div>
<a href="/delete-image/<%= img.id %>">Supprimer</a>
  

</a>
    <% }) %>

<% } else { %>

<p>Aucune image</p>

<% } %>

</body>
</html>